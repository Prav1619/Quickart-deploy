replicaCount: 2

image:
  repository: prav1619/product-service
  tag: "initial"

service:
  type: ClusterIP
  port: 8080

containerPort: 8080

resources:
  limits:
    cpu: "500m"
    memory: "512Mi"
  requests:
    cpu: "200m"
    memory: "256Mi"
